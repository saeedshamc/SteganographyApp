{
  "name": "open-stego-desktop",
  "private": true,
  "version": "0.3.6",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "icons": "tauri icon ../../assets/open-stego-icon-1024.png",
    "build:win": "tauri build --bundles nsis,msi",
    "build:linux": "tauri build --bundles deb,appimage",
    "build:deb": "tauri build --bundles deb",
    "build:nsis": "tauri build --bundles nsis",
    "build:all": "tauri build"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-opener": "^2"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "vite": "^8.0.16",
    "typescript": "~6.0.3"
  }
}
