# Place built native libraries here for bundling:
#   Windows: stego_ffi.dll
#   (copied by scripts/build-mobile-native.ps1)
#
# Android uses android/app/src/main/jniLibs/<abi>/libstego_ffi.so instead.
